{
  "name": "Highlightor",
  "version": "0.0.1",
  "description": "A firefox extension for annotating the browser's current page.",
  "repository": {
    "url": "https://github.com/Damian96/highlightor.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minifyAssets": "npm-run minify -t ./../{{filename}}.min.{{ext}} ./content-scripts/css/beautified/ && npm-run minify -t ./../{{filename}}.min.{{ext}} ./content-scripts/js/beautified/"
  },
  "author": "Damianos Giankakis",
  "license": "UNLICENSED",
  "dependencies": {
    "minifier": "^0.8.1",
    "npm-run": "^4.1.2"
  }
}
